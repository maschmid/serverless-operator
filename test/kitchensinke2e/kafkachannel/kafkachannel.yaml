apiVersion: messaging.knative.dev/{{ .version }}
kind: KafkaChannel
metadata:
  name: {{ .name }}
  namespace: {{ .namespace }}
spec:
  numPartitions: {{ .numPartitions }}
  replicationFactor: {{ .replicationFactor }}
  retentionDuration: {{ .retentionDuration }}
